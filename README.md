# Air Monitoring Application

Десктопний застосунок для моніторингу якості повітря з підключенням до PostgreSQL БД та генерацією звітів.

## Технології

- **Java 24** з модульною системою (JPMS)
- **JavaFX 21** для GUI
- **PostgreSQL** для зберігання даних
- **Gradle 8.10** для збірки
- **Apache POI** для генерації Excel звітів
- **OpenPDF** для генерації PDF звітів

## Функціональність

### ✅ Авторизація
- Вхід через логін/пароль PostgreSQL
- Збереження налаштувань у `config.properties`
- Автозаповнення останніх облікових даних
- Гарячі клавіші: Enter для переходу між полями та входу

### ✅ Перегляд даних
- Вибір таблиць з БД
- Пагінація (налаштовуваний ліміт записів)
- Опція "Показати всі" для повного завантаження
- Асинхронне завантаження з прогрес-баром та скасуванням
- Автоматична ширина стовпців
- Відображення поточного користувача
- Вибір полів для відображення

### ✅ Генерація звітів
- **Список підключених станцій** - інформація про всі станції моніторингу
- **Статистика вимірювань станції** - аналітика за вибраний період з мін/макс/середніми значеннями
- Експорт у форматах: **Excel (XLSX)** та **PDF**
- PDF з повною підтримкою кирилиці (UTF-8)
- Вибір місця збереження файлу
- Генерація в асинхронному режимі

### ✅ Візуалізація даних
- **PM2.5 та PM10 по областях** - гістограма максимальних значень PM2.5 та PM10 у розрізі областей за період
- **PM2.5 шкідливий рівень** - аналіз кількості днів з перевищенням шкідливого рівня PM2.5 (>35.4 μg/m³)
- **Діоксид сірки (SO₂)** - кругова діаграма розподілу вимірювань SO₂ за категоріями якості повітря
- **Чадний газ (CO)** - кругова діаграма розподілу вимірювань CO за категоріями якості повітря
- Табличне представлення даних разом з графіками
- Вибір періоду та станції для аналізу

### ✅ UI/UX
- Кастомний title bar (мінімізація, максимізація, закриття)
- Футуристична темна кольорова схема
- Збереження стану вікна (максимізоване/звичайне)
- Плавні анімації переходів між таблицями
- Адаптивна розмітка
- Календар вибору дат з українською локалізацією
- Модальні вікна для генерації звітів
- Гарячі клавіші (Esc - відключення)

## Структура проєкту

```
src/main/java/
├── module-info.java                    # Модульний дескриптор
└── io.github.serhii0659.air_monitoring.airmonitoringapp/
    ├── Launcher.java                   # Точка входу
    ├── HelloApplication.java           # Головний клас JavaFX
    ├── LoginController.java            # Контролер логіну
    ├── DataViewController.java         # Контролер перегляду даних
    ├── ReportsController.java          # Контролер генерації звітів
    ├── ReportGenerator.java            # Генератор Excel/PDF звітів
    ├── VisualizationMenuController.java # Контролер меню візуалізацій
    ├── PM25PM10ViewController.java     # Візуалізація PM2.5/PM10
    ├── PM25HarmfulViewController.java  # Візуалізація шкідливого PM2.5
    ├── SO2ViewController.java          # Візуалізація SO₂
    ├── COViewController.java           # Візуалізація CO
    ├── CustomTitleBar.java             # Кастомний title bar
    ├── DbManager.java                  # Менеджер БД
    ├── ConfigManager.java              # Менеджер конфігурації
    ├── DataLoadTask.java               # Асинхронне завантаження даних
    └── WindowIcons.java                # Іконки вікон

src/main/resources/
└── io.github.serhii0659.air_monitoring.airmonitoringapp/
    ├── login-view.fxml                 # FXML логіну
    ├── data-view.fxml                  # FXML перегляду даних
    ├── reports-view.fxml               # FXML генерації звітів
    ├── visualization-menu.fxml         # FXML меню візуалізацій
    ├── pm25pm10-view.fxml              # FXML візуалізації PM2.5/PM10
    ├── pm25harmful-view.fxml           # FXML візуалізації шкідливого PM2.5
    ├── so2-view.fxml                   # FXML візуалізації SO₂
    ├── co-view.fxml                    # FXML візуалізації CO
    └── styles.css                      # Стилі

config.properties.template              # Шаблон конфігурації
```

## Налаштування

### 1. База даних

Створіть БД та користувача з необхідними правами:

```sql
CREATE DATABASE air_monitoring;
CREATE USER your_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE air_monitoring TO your_user;

-- Надайте права на читання VIEW для генерації звітів
GRANT SELECT ON ALL TABLES IN SCHEMA public TO your_user;
```

Створіть необхідні таблиці та VIEW згідно зі структурою вашої БД.

### 2. Конфігурація

Скопіюйте `config.properties.template` → `config.properties`:

```properties
db.host=localhost
db.port=13000
db.name=air_monitoring
db.user=your_user
db.password=your_password
```

**⚠️ Важливо:**
- `config.properties` знаходиться в `.gitignore` - не комітиться в Git!
- Змініть порт на ваш (стандартний PostgreSQL - 5432)
- Для portable версії файл створюється автоматично поряд з EXE

## Запуск

### Через Gradle:

```bash
./gradlew.bat run
```

### Через IntelliJ IDEA:

Запустіть `Launcher.main()`

## Використання

### Вхід в систему
1. Введіть логін та пароль від PostgreSQL
2. Натисніть Enter або клацніть "Підключитися"
3. Облікові дані збережуться для наступного запуску

### Перегляд даних
1. Оберіть таблицю зі списку
2. Налаштуйте ліміт записів (10-10000)
3. Для великих таблиць: увімкніть "Показати всі" (з'явиться прогрес-бар)
4. Використовуйте пагінацію для навігації
5. Натисніть "+" для вибору полів для відображення

### Генерація звітів
1. Натисніть кнопку "Звіти"
2. Оберіть тип звіту:
   - **Список підключених станцій** - огляд всіх станцій
   - **Статистика вимірювань станції** - детальна аналітика
3. Для статистики: оберіть станцію та період (дати)
4. Оберіть формат: Excel або PDF
5. Натисніть "Згенерувати звіт" та оберіть місце збереження

### Візуалізація даних
1. Натисніть кнопку "Візуалізація"
2. Оберіть тип аналізу:
   - **PM2.5 та PM10 по областях** - порівняння максимальних значень за періодом
   - **PM2.5 шкідливий рівень** - кількість днів з перевищенням норми
   - **Діоксид сірки (SO₂)** - розподіл вимірювань за категоріями
   - **Чадний газ (CO)** - розподіл вимірювань за категоріями
3. Для візуалізацій з періодом: оберіть дати початку та кінця
4. Для візуалізацій зі станцією: оберіть конкретну станцію або "Усі станції"
5. Натисніть "Завантажити дані" для відображення графіків та таблиць

### Гарячі клавіші
- **Enter** - перехід між полями логіну / підключення
- **Esc** - відключення від БД (з вікна перегляду даних)

## Збірка

### JAR файл:

```bash
./gradlew.bat clean build
```

Результат: `build/libs/AirMonitoringApp-1.0-SNAPSHOT.jar`

### Portable EXE (працює без встановленої Java):

# Крок 1: Створи fat JAR з усіма залежностями
```bash
.\gradlew.bat clean fatJar
```
# Крок 2: Створи portable версію через jpackage
```bash
& "C:\Program Files\Java\jdk-24\bin\jpackage.exe" `
  --input build/libs `
  --name AirMonitoringApp `
  --main-jar AirMonitoringApp-1.0-all.jar `
  --main-class io.github.serhii0659.air_monitoring.airmonitoringapp.Launcher `
  --type app-image `
  --dest build/portable
```

**Результат:** `build/portable/AirMonitoringApp/` (~150-200MB)
- Містить `AirMonitoringApp.exe` + JRE + всі бібліотеки
- Просто скопіюй папку `AirMonitoringApp` і запускай EXE
- **Працює на будь-якому Windows без встановленої Java!**
- Не потребує інсталяції - portable версія

**⚠️ Перший запуск:**
При першому запуску додаток автоматично створить файл `config.properties` поряд з EXE.
Відкрий його та заповни параметри підключення до PostgreSQL:
```properties
db.host=localhost
db.port=5432
db.name=air_monitoring
db.user=your_username
db.password=your_password
```
Після збереження перезапусти додаток.

## Залежності

### Основні
- `org.postgresql:postgresql:42.7.3` - PostgreSQL JDBC драйвер
- `org.openjfx:javafx-controls:21.0.6` - JavaFX контроли
- `org.openjfx:javafx-fxml:21.0.6` - JavaFX FXML

### UI компоненти
- `org.controlsfx:controlsfx:11.2.1` - Додаткові контроли
- `org.kordamp.ikonli:ikonli-javafx:12.3.1` - Іконки
- `eu.hansolo:tilesfx:21.0.9` - Додаткові компоненти

### Генерація звітів
- `org.apache.poi:poi:5.5.0` - Основна бібліотека Apache POI
- `org.apache.poi:poi-ooxml:5.5.0` - Підтримка XLSX формату
- `com.github.librepdf:openpdf:1.3.30` - Генерація PDF з підтримкою UTF-8

## Особливості реалізації

### 🔒 Безпека
- Використання PreparedStatement для SQL запитів
- Виведення чутливих даних у окремий config файл

### ⚡ Продуктивність
- Асинхронне завантаження даних (не блокує UI)
- Пагінація для великих таблиць
- Опціональне обмеження сортування для великих датасетів (>5000 записів)
- Lazy loading стовпців
- Інтелектуальне відображення прогрес-бару (з'являється лише при довгому завантаженні)

### 🎨 UI/UX
- Undecorated window з кастомним title bar
- Плавні fade анімації при зміні таблиць
- Hover ефекти на кнопках
- Responsive design
- Автофокус на полях вводу
- Гарячі клавіші (Enter, Esc)
### 📊 Робота з даними
- Автоматичне визначення типів стовпців
- Розумна ширина стовпців (ID - вузькі, описи - ширші)
- Підтримка NULL значень
- Форматування чисел з комами
### 📄 Генерація звітів
- Excel з автоматичним розміром стовпців та стилізацією
- PDF з landscape орієнтацією для широких таблиць
- Підтримка кирилиці через Arial (Windows) або Times Roman
- Підсумкові рядки з агрегованими даними

## Відомі обмеження
- Сортування вимкнено для таблиць >5000 записів (для продуктивності)
- Автоматичне розширення стовпців вимкнено для великих таблиць
- Тільки перегляд даних (без редагування)
- PDF використовує системний шрифт Arial (повинен бути встановлений в Windows)

## Версії

**v1.0** (2025-11-19)
- ✅ Авторизація через PostgreSQL
- ✅ Перегляд таблиць з пагінацією
- ✅ Генерація звітів (Excel + PDF)
- ✅ Футуристичний дизайн
- ✅ Гарячі клавіші та автофокус

## Ліцензія

Освітній проєкт

## Автор

Serhii (serhii0659) - GitHub: [@serhii0659](https://github.com/serhii0659)
